# Data

This folder keeps only data needed by the active project.

- `cities/` - Germany and France city coordinate inputs.
- `costs/` - tower-radius cost function data.

Older filtering artifacts were moved to `trash/data_archive/`.
